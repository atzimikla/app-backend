# Validacion del PR: secrets scan + Dockerfile lint + SCA + build + smoke test.
# No requiere credenciales AWS, sirve para PRs sin que ADO pida la SC.

jobs:
  - job: Validate
    displayName: Build image (no push)
    pool:
      vmImage: ubuntu-latest
    steps:
      - checkout: self

      - script: |
          set -euo pipefail
          GITLEAKS_VERSION="v8.21.2"
          curl -fsSL "https://github.com/gitleaks/gitleaks/releases/download/${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION#v}_linux_x64.tar.gz" \
            | tar -xz -C /tmp gitleaks
          sudo mv /tmp/gitleaks /usr/local/bin/
          gitleaks detect --no-git --source . --verbose --redact --exit-code 1
        displayName: Gitleaks secret scan

      - script: |
          set -euo pipefail
          HADOLINT_VERSION="v2.12.0"
          curl -fsSL "https://github.com/hadolint/hadolint/releases/download/${HADOLINT_VERSION}/hadolint-Linux-x86_64" \
            -o /tmp/hadolint
          chmod +x /tmp/hadolint
          sudo mv /tmp/hadolint /usr/local/bin/
          hadolint Dockerfile
        displayName: Hadolint Dockerfile lint

      - script: |
          set -euo pipefail
          # SCA a nivel source: revisa requirements.txt contra PyPI Advisory DB.
          # Falla en cualquier vulnerabilidad encontrada (por defecto pip-audit
          # corta en exit 1). Ajustar threshold si genera ruido.
          pip install --no-cache-dir pip-audit==2.7.3
          pip-audit -r requirements.txt
        displayName: pip-audit (Python deps SCA)

      - script: |
          set -euo pipefail
          docker build --tag app-backend:ci-validate .
          docker run --rm app-backend:ci-validate python -c "import app; print('import OK')"
        displayName: docker build + smoke test

      - script: |
          set -euo pipefail
          # SCA a nivel container: scan de OS packages + langauge deps de la
          # imagen recien buildeada. Bloquea solo en HIGH/CRITICAL CON fix
          # upstream disponible (--ignore-unfixed evita ruido por CVEs que no
          # podemos accionar todavia).
          TRIVY_VERSION="0.57.0"
          curl -fsSL "https://github.com/aquasecurity/trivy/releases/download/v${TRIVY_VERSION}/trivy_${TRIVY_VERSION}_Linux-64bit.tar.gz" \
            | tar -xz -C /tmp trivy
          sudo mv /tmp/trivy /usr/local/bin/
          trivy image \
            --severity HIGH,CRITICAL \
            --ignore-unfixed \
            --exit-code 1 \
            --no-progress \
            app-backend:ci-validate
        displayName: Trivy container scan (SCA)
